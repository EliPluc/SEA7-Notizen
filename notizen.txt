das sind die 1. Notizen für GitHub

GIT > Versionskontrollsystem

git Status -> Sicht auf die Datein

git add -> wird von git berücksichtigt

git commit ->schließt Sicherungsvorgang ab

git log
compare & pull request = merge
 
mit dem Befehl git fetch werden Commits, Dateien, und Verweise aus einem Remote-Respository heruntergeladen.Mit dem Befehl git Checkout explizit ausgecheckt werden 
